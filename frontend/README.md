# Frontend

Frontend is written in ReactJS and gets served using nginx proxy, config can be found [here](../webserver)

## Quickstart


```shell
# For compiling code
make build
```

```shell
To run the code
serve -s build
```

```shell
make image
```
